<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RonnysTheme" parent="Theme.AppCompat.Light.DarkActionBar" >
        <item name="android:colorPrimary">@color/TopCoat</item>
        <item name="android:colorPrimaryDark">@color/TopCoat</item>
        <item name="android:colorAccent">@color/BaseCoat</item>
        <item name="android:colorBackground">@color/nextCoat</item>
        <item name="android:navigationBarColor">@color/nextCoat</item>
        <item name="colorPrimary">@color/TopCoat</item>
        <item name="colorAccent">@color/BaseCoat</item>
        <item name="colorPrimaryDark">@color/TopCoat</item>
        <item name="android:colorForeground">@color/nextCoat</item>
        <item name="android:textColorPrimary">@android:color/background_light</item>
        <item name="android:textColorPrimaryInverse">@android:color/transparent</item>
        <item name="android:textColorSecondary">@color/BaseCoat</item>
    </style>

</resources>

